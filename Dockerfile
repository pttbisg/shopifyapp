FROM node:12-alpine

RUN mkdir -p /usr/src/app
WORKDIR /usr/src/app

# Installing dependencies
COPY package*.json /usr/src/app/
RUN npm install

# Copying source files
COPY . /usr/src/app

RUN npm run build

EXPOSE 8081

CMD npm run start